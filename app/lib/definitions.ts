export type Dictionary = {
  siteHeader: {
    nav: {
      links: {
        about: string;
        skills: string;
        projects: string;
        contact: string;
      }
    }
  }
};

export type LangParams = {
  lang: 'en' | 'pt';
};
